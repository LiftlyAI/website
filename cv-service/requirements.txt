# opencv comes transitively from mediapipe (opencv-contrib-python) — do not
# also pin opencv-python-headless or the two fight over cv2.pyd on Windows.
fastapi==0.115.5
uvicorn[standard]==0.32.1
mediapipe==0.10.35
numpy==2.1.3
scipy==1.14.1
python-multipart==0.0.17
